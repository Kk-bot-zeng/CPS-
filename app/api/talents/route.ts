import { NextResponse } from "next/server";
import { requireApiUser } from "@/lib/api-auth";

export async function GET(request:Request){const auth=await requireApiUser();if(auth.error)return auth.error;const q=new URL(request.url).searchParams.get("q")||"";let query=auth.admin.from("talents").select("*,leaders(name)").order("created_at",{ascending:false});if(q)query=query.ilike("name",`%${q}%`);const{data,error}=await query;if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data)}
export async function POST(request:Request){const auth=await requireApiUser();if(auth.error)return auth.error;const body=await request.json();if(!body.name)return NextResponse.json({error:"达人昵称不能为空"},{status:400});const{data,error}=await auth.admin.from("talents").insert(clean(body)).select().single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json(data,{status:201})}
function clean(b:Record<string,unknown>){return{name:b.name,platform:b.platform||null,platform_account:b.platform_account||null,phone:b.phone||null,wechat:b.wechat||null,leader_id:b.leader_id||null,province:b.province||null,city:b.city||null,district:b.district||null,address:b.address||null,longitude:b.longitude||null,latitude:b.latitude||null,cooperation_status:b.cooperation_status||"合作中",tags:b.tags||[],notes:b.notes||null,updated_at:new Date().toISOString()}}
